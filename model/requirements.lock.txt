# An environment known to work, pinned.
#
#   python3 -m venv .venv                             # the version named below
#   .venv/bin/pip install -r requirements.lock.txt
#   .venv/bin/pip install -e /path/to/QDrive          # not installable from here
#
# Generated with `pip freeze` from the environment the game was developed
# against. requirements.txt is the readable one and takes current releases;
# this is for reproducing that environment exactly, or for working out what
# changed when a new release breaks something.
#
# Python 3.14.2. QDrive needs 3.12 or newer; qiskit is fussy about patch
# releases besides, so pin the interpreter as firmly as these.
#
# The two private checkouts are not lines here — neither is installable from a
# clone of this repository — but the commits that environment used were:
#
#   QDrive       github.com/moth-quantum/QDrive      a09988f5ad1d6c51b0f99fc3530ce6c4aac1bb21
#   qdrive-api   github.com/moth-quantum/qdrive-api  1b897e2  (engine.yaml version 0.1.3)
#
# Regenerate with:  pip freeze | grep -v '^-e ' | sort
#
# Never commit a `pip freeze` line for a package installed from a URL carrying a
# credential: pip keeps the URL verbatim in the installed metadata, so the token
# comes back out of every freeze. Strip those lines, as this file does.

annotated-types==0.8.0
antlr4-python3-runtime==4.13.2
dill==0.4.1
numpy==2.5.2
openqasm3==1.0.1
psutil==7.2.2
pydantic==2.13.5
pydantic_core==2.46.5
python-dateutil==2.9.0.post0
qiskit-aer==0.17.2
qiskit-qasm3-import==0.6.0
qiskit==2.5.2
rustworkx==0.18.1
scipy==1.18.1
six==1.17.0
stevedore==5.9.1
typing-inspection==0.4.4
typing_extensions==4.16.0
